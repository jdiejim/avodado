---
"@avodado/cli": patch
"@avodado/core": patch
---

Improve the npm READMEs. The CLI README now opens with a worked example (a
Markdown doc with a `sequence` block → `avo check` / `avo html` / `avo studio`)
and plainer language; the core README lists the real block-type set (77 across
12 families) instead of a stale short list.
