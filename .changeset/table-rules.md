---
'chiltepin-render': patch
'chiltepin-studio': patch
---

Tables have a visible frame and row rules again: a new `--rule-table` token (32% ink in light, 28% in dark) replaces the 13% hairline on every table kind, and the plain `table` block gets a 1px rounded border.
