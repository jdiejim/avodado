---
'avodado': minor
---

`avo mcp` and the `@avodado/mcp` package are removed. Agents get everything through the skill (`npx skills add jdiejim/avodado`) and the CLI; tools with only a system-prompt box use `avo skill`.
