---
"avodado": patch
---

`avo --version` (and the studio's version reporting) works again after the
package rename — the version lookup accepted only the old `@avodado/cli` name
and fell back to `0.0.0`.
