## What

One sentence.

## Checks

- [ ] `pnpm typecheck && pnpm test && pnpm lint` green
- [ ] `node packages/cli/dist/bin.js check` clean
- [ ] Changeset added (anything a user can see)
- [ ] For a block: schema + renderer + skill entry + catalog example + test, and an eval scenario
- [ ] A rendered example attached for anything visual
